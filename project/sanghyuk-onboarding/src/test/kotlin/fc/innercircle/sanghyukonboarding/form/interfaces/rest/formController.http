### Create a new form
POST http://localhost:8080/api/v1/forms
Content-Type: application/json

{
  "title": "커피 선호도 조사",
  "description": "어떤 커피를 가장 선호하는지 알려주세요.",
  "questions": [
    {
      "title": "주로 마시는 커피 종류는?",
      "description": "아래 옵션 중 하나를 선택하세요.",
      "type": "CHECKBOX",
      "required": true,
      "selectableOptions": [
        {
          "text": "아메리카노"
        },
        {
          "text": "카페라떼"
        },
        {
          "text": "카푸치노"
        }
      ]
    },
    {
      "title": "하루에 커피를 몇 잔 마시나요?",
      "description": "",
      "type": "TEXT",
      "required": false,
      "selectableOptions": []
    }
  ]
}

> {%
    console.log(`headers: ${JSON.stringify(request.headers.all())}`)
    console.log(`request body: ${JSON.stringify(request.body)}`)
%}

### Get Form by ID
GET http://localhost:8080/api/v1/forms/01JXSPX86PZCG0CM7PMGR8MS3J
Accept: application/json

> {%
    console.log(`headers: ${JSON.stringify(request.headers.all())}`)
    console.log(`request body: ${JSON.stringify(request.body)}`)
%}
